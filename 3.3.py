#Recursive Algorithm: O(n log n) - This is because, in each recursive call, we are partitioning the array into two subarrays.
#Non-Recursive Algorithm: O(n) - Both list comprehensions run in linear time.